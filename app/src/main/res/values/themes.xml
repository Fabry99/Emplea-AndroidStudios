<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Emplea" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Emplea" parent="Base.Theme.Emplea" />

    <style name="SplashTheme" parent="Base.Theme.Emplea">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="Theme.Emplea.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Emplea.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Emplea.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>